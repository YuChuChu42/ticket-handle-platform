# 智链工单系统 V2.0 - 测试指南

## 🎯 系统概述

这是一个面向企业级路由器技术支持的工单管理系统，支持三种角色：
- **管理员（admin）**：分配工单、查看所有数据
- **技术人员（technician）**：处理工单、修改状态、生成PDF报告
- **负责人（reporter）**：创建工单、上传图片、设置优先级

---

## 🚀 快速启动

### 1. 数据库准备

```bash
# 已在 pgAdmin4 中执行：
# - database/schema_v2.sql
# - database/seed_v2.sql

# 初始化密码哈希
cd backend
npm run init-db-v2
```

### 2. 启动服务

```bash
# 后端（端口 3000）
cd backend
npm run dev

# 前端（端口 5173）
cd frontend
npm run dev
```

### 3. 访问系统

- **前端地址**：http://localhost:5173
- **后端API**：http://localhost:3000

---

## 👥 测试账号

所有账号密码均为：**password123**

### 管理员
| 用户名 | 角色 | 权限 |
|--------|------|------|
| admin | 管理员 | 分配工单、查看所有工单 |

### 技术人员
| 用户名 | 姓名 | 权限 |
|--------|------|------|
| tech1 | 技术员张工 | 查看分配的工单、修改状态、生成报告 |
| tech2 | 技术员李工 | 查看分配的工单、修改状态、生成报告 |

### 负责人（客户）
| 用户名 | 公司 | 权限 |
|--------|------|------|
| reporter1 | 北京分公司 | 创建工单、上传图片、修改优先级、删除待处理工单 |
| reporter2 | 上海分公司 | 创建工单、上传图片、修改优先级、删除待处理工单 |
| reporter3 | 深圳分公司 | 创建工单、上传图片、修改优先级、删除待处理工单 |

---

## 🧪 功能测试流程

### 测试场景 1：负责人创建工单

**登录**：`reporter1` / `password123`

1. 点击"创建工单"按钮
2. 填写工单信息：
   - **标题**：路由器频繁断网问题
   - **问题描述**：办公室路由器每隔2小时自动断网重启，影响正常办公
   - **问题地点**：北京市朝阳区建国路88号3楼
   - **对接电话**：13912345678
3. 上传现场图片（最多4张，单张不超过5MB）
4. 选择优先级：**紧急**
5. 点击"创建工单"

**预期结果**：
- ✅ 工单创建成功
- ✅ 自动跳转到工单详情页
- ✅ 状态为"待处理"
- ✅ 图片正常显示

---

### 测试场景 2：管理员分配工单

**登录**：`admin` / `password123`

1. 进入工单列表
2. 点击刚创建的工单"查看详情"
3. 在右侧"工单分配"面板中：
   - 选择技术人员：**技术员张工**
   - 点击"分配工单"

**预期结果**：
- ✅ 分配成功提示
- ✅ 工单显示"技术员张工"
- ✅ 评论区自动添加分配记录

---

### 测试场景 3：技术人员处理工单

**登录**：`tech1` / `password123`

1. 进入工单列表（只显示分配给自己的工单）
2. 点击工单"查看详情"
3. 修改状态：
   - 选择"处理中"
   - 点击"更新状态"
4. 添加评论：
   - 输入："正在远程诊断问题，请保持路由器在线"
   - 点击"发表评论"
5. 继续修改状态为"已解决"

**预期结果**：
- ✅ 状态更新成功
- ✅ 评论添加成功
- ✅ 右侧出现"生成报告"按钮

---

### 测试场景 4：生成和下载PDF报告

**继续上一场景（tech1登录）**

1. 在工单详情页，点击"生成PDF报告"
2. 等待报告生成（约2-3秒）
3. 生成成功后，点击"下载PDF报告"

**预期结果**：
- ✅ PDF报告生成成功
- ✅ 下载PDF文件
- ✅ PDF内容包含：
  - 工单编号、报告日期
  - 基本信息（公司、地点、电话）
  - 处理过程记录
  - 问题诊断与处理结果
  - 技术说明
  - 责任声明与法律效力
  - 签字栏

---

### 测试场景 5：负责人查看处理结果

**登录**：`reporter1` / `password123`

1. 进入工单列表
2. 查看自己创建的工单状态（已解决）
3. 点击"查看详情"
4. 下载PDF报告
5. 添加评论："测试通过，问题已解决，感谢！"

**预期结果**：
- ✅ 可以看到完整的处理过程
- ✅ 可以下载PDF报告
- ✅ 可以添加评论
- ✅ 可以修改优先级
- ✅ 不能删除已处理的工单

---

### 测试场景 6：负责人删除待处理工单

**登录**：`reporter1` / `password123`

1. 创建一个测试工单
2. 在工单列表中，待处理状态的工单显示"删除"按钮
3. 点击"删除"，确认删除

**预期结果**：
- ✅ 待处理工单可以删除
- ✅ 删除后从列表中消失
- ✅ 已处理的工单没有删除按钮

---

## 📊 数据验证

### 1. 检查数据库

```sql
-- 查看用户角色分布
SELECT role, COUNT(*) FROM users GROUP BY role;

-- 查看工单状态分布
SELECT status, COUNT(*) FROM tickets GROUP BY status;

-- 查看图片上传情况
SELECT ticket_id, COUNT(*) as image_count FROM ticket_images GROUP BY ticket_id;

-- 查看PDF报告生成情况
SELECT COUNT(*) FROM tickets WHERE report_file IS NOT NULL;
```

### 2. 检查文件存储

```bash
# 查看上传的图片
ls -lh backend/uploads/images/

# 查看生成的PDF报告
ls -lh backend/uploads/reports/
```

---

## 🎨 UI/UX 检查点

### 工单创建页面
- [ ] 表单字段布局清晰
- [ ] 图片上传组件正常（预览、删除）
- [ ] 文件大小验证（5MB限制）
- [ ] 图片数量限制（最多4张）
- [ ] 手机号格式验证
- [ ] 草稿自动保存

### 工单列表页面
- [ ] 分页功能正常
- [ ] 搜索功能正常（标题、描述、地点）
- [ ] 状态筛选（pending、in_progress、resolved、closed）
- [ ] 优先级筛选（low、mid、high、urgent）
- [ ] 角色权限按钮显示正确

### 工单详情页面
- [ ] 图片展示和预览（点击放大）
- [ ] 评论区实时更新
- [ ] 分配面板（管理员可见）
- [ ] 状态管理面板（技术人员可见）
- [ ] 优先级设置（负责人可见）
- [ ] PDF下载按钮
- [ ] 权限提示清晰

---

## ⚠️ 已知限制

1. **文件存储**：当前使用本地存储，生产环境建议使用云存储（阿里云OSS/腾讯云COS）
2. **PDF字体**：PDFKit使用默认字体，中文显示可能需要额外配置
3. **图片大小**：建议在上传前压缩大图片
4. **并发测试**：未进行大规模并发测试

---

## 🔧 故障排查

### 前端无法连接后端

```bash
# 检查后端是否运行
lsof -i:3000

# 检查环境变量
cat frontend/.env
# 应包含：VITE_API_BASE=http://localhost:3000
```

### 图片上传失败

```bash
# 检查上传目录权限
ls -la backend/uploads/

# 检查multer配置
cat backend/src/config/upload.js
```

### PDF生成失败

```bash
# 检查pdfkit是否安装
npm list pdfkit

# 查看后端日志
cd backend && npm run dev
```

### 数据库连接失败

```bash
# 检查PostgreSQL是否运行
psql -U postgres -d ticket_system -c "\dt"

# 检查环境变量
cat backend/.env
```

---

## 📝 升级到云存储（可选）

如果需要升级到阿里云OSS：

1. 安装依赖：
```bash
cd backend
npm install ali-oss
```

2. 创建 `backend/src/config/oss.js`：
```javascript
const OSS = require('ali-oss');

const client = new OSS({
  region: 'your-region',
  accessKeyId: process.env.OSS_ACCESS_KEY_ID,
  accessKeySecret: process.env.OSS_ACCESS_KEY_SECRET,
  bucket: 'your-bucket-name',
});

module.exports = client;
```

3. 更新上传逻辑，将文件上传到OSS而非本地

---

## 🎉 完成！

系统已完全升级到企业级V2.0版本，支持：
- ✅ 三种角色权限体系
- ✅ 图片上传（最多4张）
- ✅ PDF报告自动生成（具有法律效力）
- ✅ 工单分配流程
- ✅ 完整的状态管理
- ✅ 实时评论功能

如有问题，请检查：
- 数据库是否正确初始化
- 后端和前端是否正常启动
- 环境变量是否配置正确

